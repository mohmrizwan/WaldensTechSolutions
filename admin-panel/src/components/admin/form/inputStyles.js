// Shared className strings for form inputs/textareas so every form
// (Service, Project, Team, Settings) stays visually consistent.
export const inputClass =
  'w-full rounded-lg border border-white/10 bg-surface-200/50 px-3 py-2 text-sm text-gray-100 outline-none transition placeholder-gray-500 focus:border-accent/50 focus:bg-surface-200/80'

export const textareaClass = `${inputClass} resize-none`
